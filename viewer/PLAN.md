# Viewer plan and status (Dev B)

Companion to `CLAUDE.md`, `docs/CONTRACT.md` and "Viewer design notes (Dev B)" in the master design doc. Update the status table when a step lands.

## Status

| Step | What | State |
| --- | --- | --- |
| B0 | Repo skeleton for all lanes, Vite scaffold, Pages workflow | done, `main` 840fb42 |
| B1 | `src/format.js` parser, Node encoder, `make-scene.mjs`, `validate-scene.mjs`, tests, committed `box` scene | done, `main` 43a4deb |
| B2 | scene loader, GPU reveal + staleness shader, replay clock, HUD, commander mode | done, `main` 001e2bf. Agent verified commander rendering/top-down/HUD in Chrome. Dev B confirmed box rendering and HUD in Samsung AR on 2026-09-19; human commander check remains pending |
| B3 | AR mode on the Samsung: WebXR session, `local` space, HUD as dom-overlay, fps budget | implemented, `main` 5ce7d44 (committed/pushed by Dev B). Dev B confirmed camera/box/HUD and stable anchoring on S21 Ultra. First 200k stress run: 30.01 avg / 29.83 min half-second fps, XR 1080 x 2400, fbscale 1, no errors/resets. Exit button restores Enter correctly. **Budget table complete 2026-09-19**: 400k 29.98/29.69, 800k (774,018 drawn) 30.00/29.79, unlimited 29.99/29.85 avg/min fps, all at 1080 x 2400 fbscale 1, no resets; the phone is camera-capped at 30 fps up to the whole stress scene, so the ceiling is above 774k points (SAMSUNG.md). Exit/re-entry exercised on the same page. One failed first Enter AR tap ("session configuration is not supported") right after a navigation out of a live session; second tap worked |
| B4 | alignment nudges (x, y, z 1 cm, yaw 0.5 deg), alignment vs x-ray mode, localStorage + URL override | implemented, `main` 5ce7d44. 19 tests + build and Chrome smoke pass. Dev B confirmed X/Y/Z nudges, yaw and x-ray switching. Agent verified phone Save/reload persistence and temporary URL overrides without overwriting stored offsets. **Control checks passed; physical alignment was not attempted (B5)** |
| B5 | first real scene from A: validator, alignment within ~10 cm at the jig | **Real scenes exist** (2026-09-19 ~23:50 UTC) on C's branch `feat/people-run-and-spark-renderer`: `room013` (335,568 static + 346,632 alignment points, 45 s, 24.6 MB, no people) and `room012_masked` (198,867 static + 170,777 alignment points, 426 ghost frames / 832,937 points, 54 s, 26.6 MB). Both pass our validator (exit 0; room013 warns only for missing people files; both first poses are yaw-free with 3-8 deg tilt, which the old identity rule would have rejected) and load headless with no errors; ghost seen at 20 s on room012_masked. **Pending: the jig, wall distance and room from A, then the phone alignment check.** Earlier note: waiting for a recording (`data/raw/` empty on 2026-09-19 20:40 UTC). De-risked: A's exporter run end to end on their synthetic-take tests writes scenes our validator accepts (exit 0; warnings only for the empty alignment file and missing people files) and `npm run load -- <scene>` renders one with no errors. Acceptance recipe: `node scripts/validate-scene.mjs public/scenes/<scene>` exit 0, then `npm run load -- <scene>` exit 0 and look at its screenshot, then the phone at the jig |
| B6 | person ghosts with hold, 15 s fade, "person, last seen N s ago" label; staleness legend | implemented 2026-09-19: `src/ghosts.js` + pure `ghostStateAt` in `scene-data.js` (tested). Seen while the latest people frame is under 0.75 s old, then held and faded over 15 s; label sprite at the centroid; additive red-orange, depthTest off so it shows through walls. Headless smoke verifies seen / fading / hidden states and screenshots on C's `fake` scene. **Seen in AR on the S21 Ultra 2026-09-19** (`?mode=ar&scene=fake&t=4`): Dev B saw the labelled ghost through the wall; the USB log shows seen 5-12 s (2,000 points, alpha 1), fade to 0.57 by 19 s with last-seen 7 s, hidden after the loop, 30 fps, no errors |
| B7 | portal (gaze-following hole in the wall plane) and responder frustum + trail | implemented 2026-09-19: `src/portal.js` (depth-only oversized plane at wall_z + 0.02 with a 0.6 m hole, translated to where the camera's forward ray meets the wall plane in sceneRoot space; additive cyan ring; enabled only in AR x-ray mode, or `?portaldebug=1` in commander) and `src/responder.js` (frustum at the interpolated pose, trail via draw range; depthTest off). Pure `poseAt` / `intersectWallPlane` in `scene-data.js`, tested. Headless smoke: responder at 12 s (pose 120, 121 trail points) and portal-on vs portal-off screenshots at 20 s show the room only inside the ring. **Portal seen in AR on the S21 Ultra 2026-09-19**: the ring followed the gaze and the USB log shows the hit on z = -1.8 in every frame with no misses. Align-wall toggle not exercised in AR (smoke covers the logic) |
| B8 | commander polish (top-down, scrubber, caption), iPad Safari check, Pages URL + QR | polish implemented 2026-09-19: `?look=xray` (default; cyan fresh -> dim blue stale, luminance kept) / `color` (recorded RGB) / `blueprint` (dark-on-white, commander); HUD look selector; commander **cutaway** (default on: clips points above 0.9 m and within 8 cm behind the wall plane, hides the outside wall) with a HUD toggle and `?cutaway=0`. Smoke screenshots per look incl. top-down. Deploy workflow reordered so the build job passes before Pages is enabled. **Deployed 2026-09-19 19:44 UTC**: after the Pages source was set to GitHub Actions, run 35465234456 (B8 push, build + deploy jobs green) published <https://michaelyeh507.github.io/OmniSight/?mode=commander&scene=fake>; it serves the same bundle hash as a local build and `OMNI_URL=https://michaelyeh507.github.io/OmniSight/ npm run smoke` passes against it (stress caps skipped there: that scene is gitignored). `public/qr.png` from `npm run qr` encodes that commander URL (decoded back with OpenCV). Deployed again as dc72fee (run 35467321213 green, bundle hash matches the local build, `qr.png` served). Dev B scanned the QR on 2026-09-19 and saw the commander view load (device to confirm as the iPad Safari check). **Pending: regenerate the QR when a hero scene replaces `fake`** |
| B9 | bug duty on hero scenes, perf tuning with A, feature freeze at hour 16 | started 2026-09-19 from the phone logs: (1) Enter AR retries once, 0.8 s later, when Chrome answers `NotSupportedError` right after a previous session (seen on the S21 Ultra; a denial is never retried); (2) the portal hole is clamped to 3 m from the point straight ahead, so a grazing gaze (108 m hit in the USB log) no longer drags the occluder off the wall. Both unit-tested; smoke and build pass. Not yet re-verified on the phone. (3) Multi-source responders: a merged two-take scene (A's CLI already writes them) drew one frustum interpolating across both walkers; now one frustum + trail + label per source, tested on the committed `two` fixture and in `test/responder.test.js`. (4) Clean capture for the demo video: `?capture=1` or the `h` key hides every HUD panel except the honesty caption (smoke-checked); in AR leave the session with Android Back. (5) `npm run phone -- --watch 5 --for 10` heat-soak log. (6) **World-frame bug found in our validator 2026-09-19 evening**: it demanded an identity first pose, which is only true for a perfectly level jig; Dev A's branch `fix/offline-trajectory-registration` (da85503) changed normalization from yaw-only to the full frame-0 basis to satisfy it, which tilts the whole replay off gravity whenever the jig phone is pitched. Validator now requires zero position and zero yaw, allows pitch/roll (WARN above 10 deg), rejects a vertical first frame; `firstPoseHeading` exported and tested. `__omni.firstPose` (position, quaternion, tiltDeg of the first tracked XR pose) added to settle empirically whether Chrome's `local` space is gravity-aligned. **A must keep yaw-only normalization** |
| B10 | AR UI for the demo: x-ray on/off with a portal iris, the portal on any wall plane, HUD legibility pass, phone-screen renders with the HUD over real footage | implemented 2026-09-20 (session 3), 34 tests + build + smoke green, not yet on the phone. `?xray=0` / HUD **X-ray** button / key `x`: camera view (nothing recorded drawn, legend hidden) vs x-ray; the portal hole irises open over 0.45 s. `?wall=-x:-0.69` puts the portal on room012's door wall (any plane: `[-]x|y|z:value` or `nx,ny,nz,d`; default `z:wall_z`); in x-ray the wall's own points (14 cm) and everything outside are clipped when the camera is outside. `?ui=ar` is the phone-screen preview for renders (AR HUD, transparent page). `npm run render` now defaults to that HUD at dpr 2, takes `--xray-at`, `--bg <clip>` (footage seeked per frame behind the page, `--bg-offset/--bg-stretch`) and `--stills t,t`. Frames checked before / during / after the switch for the corridor and first-person shots; lens check with recorded colours over the take's footage: 43.6 deg vertical fov matches the 16:9 crop of the iPhone's 1920x1440 (fx 1348). Rendered: `room012_corridor_ui_placeholder.mp4` (stand-in wall) and `room012_follow_ui.mp4` (the take's own footage). **Waiting for the real outside-wall clip; then one `npm run render ... --bg <clip>` replaces the composite step** |
| B11 | hero-demo sketch: object outlines (the reference's red wireframes) and the team map | implemented 2026-09-20 (session 3, after the user's priority shift): `<scene>/outlines.json` (viewer-side, optional) lists hand-annotated boxes `{label, center, size, yaw, t?}`; `src/outlines.js` draws each as a hot-red fat-line wireframe (LineSegments2, 2.5 px core + 9 px additive glow, depthTest off so it shows through the wall) with a name tag, drawing itself in over 0.6 s at `t` (default: when the map first saw the box's own points) and settling to 55 % opacity by 30 s of age; hidden in the camera view. `scripts/fit-outlines.mjs <scene> --png <file>` clusters furniture cells (band 0.12-1.4 m above the floor, walls = floor-to-ceiling cells) into candidate boxes and a top-down picture; the human then names and trims them (`room012_video/outlines.json`: bed, desk, chair, bins, dresser, wardrobe, window; `box/outlines.json`: two fixture boxes for smoke). `src/minimap.js` = the team map panel top-right: occupancy grid of the static map (0.25-1.9 m above the floor) coloured by data age and revealed with the replay, the wall plane dashed, every responder with heading and `R<n>`, the person, and `YOU` (the XR camera or the render camera in sceneRoot space); replay-time throttled to 8 Hz. Legend row OUTLINES / hand-annotated objects. 37 tests, build, smoke (sketch + map on `box`) green; corridor stills in `omni-render/stills-sketch/`. Not on the phone yet |
| B12 | hero demo: the walk-in take as two phone screens (first POV walking in, second POV in the corridor watching the wall light up) | implemented 2026-09-20 02:00-03:00. Scene `hero` = take `c9ae63f0c4_2` exported with `--camera-convention opencv`, masks from `people.run` (same flag), voxel 0.035, stride 4: 253k static points, 813 ghost frames, 57.5 s, floor -1.47; validator OK. Geometry (recording frame): corridor 1.1 m wide along Z from the origin, walker at z = -3.6 by 12 s, door at z ~ -3.1, room at x > 0.46 (wall `-x:0.46`), x 0.46..4.1, z -3.98..-0.87; person at the desk (3.0, -0.6, -2.6) from 14 s, walking 28-40 s and 48-57 s. Hand outlines: bed, desk, chair, window, dresser. Second POV: `cam=fixed&campos=-0.6,0,-2.6&camat=4,-0.5,-2.6&wall=-x:0.46&portalr=0.42&fov=55`, x-ray from the start (empty ring for 15 s, then the room paints in as R1 scans it, R1 and the person on the team map). First POV: `cam=follow&wall=-x:0.46`, fov 43.6, footage `rgb.mp4` stretched x1.00183, x-ray at 14 s (the door). New: `?portalr=`, portal rim 6 % of the radius, responder trails clipped at the wall plane in x-ray (three.js clipping planes; the path beside the viewer no longer cuts across the frame). 38 tests, build, smoke green. Renders: `hero_corridor_ui.mp4`, `hero_follow_ui.mp4` (stills in `stills-hero-corridor3/`, `stills-hero-follow/`). **Real second-POV clip** (user, 03:05): `Downloads/18fa8d26b28f49c6bcc7bfc20b617fac.mov`, 536x954 portrait, 30 fps, 14.3 s, hand-held, from the corridor about 2 m from the door wall (door open at the left, blank wall with the 314D sign right); the teammate stands at the door, steps through at clip ~7 s, and walks back out at 13-14 s. Sync: clip 0 = hero 9 s (`--bg-offset -9`), held at clip 12.6 s (empty doorway, `--bg-end`) afterwards. Viewpoint fitted by blending colour renders over the clip frame: `campos=-1.6,0.05,-2.75 camat=0.46,-0.25,-3.05 fov=71 portalr=0.5`, portrait 1080x1920 at dpr 2.5. Output `hero_pov2_portrait.mp4` (hero 9-57.5 s). Not verified: the camera was hand-held, so the layer drifts a little against the footage; a Stray Scanner recording of the second phone (asked of the user) would make it exact and allow panning |
| B13 | user feedback round (2026-09-20 03:30): moving second POV, minimalist Eagle Eye HUD, cleaner x-ray | implemented 03:30-04:30. **Soft x-ray** (`?portal=soft`, default; `ring` keeps the old hard hole and cyan rim): no occluder, the points shader fades the room in around the gaze hit through a screen-space feathered spot (`uHole` = centre px, radius px, feather; projected per view in `onBeforeRender`, so it works per XR eye), irising with the reveal; outlines are masked by the same spot (LineMaterial `onBeforeCompile`) and their tags gated on it; ghosts, trails and frustums stay visible through the whole wall. A tiny reticle marks the gaze. **Minimal HUD** (index.html rewrite): no panels or borders, hairlines, mixed-case regular type at 10-13 px CSS (1-1.5 % of the frame on the phone), subtle text shadow only; top-left mode word + two outlined actions, top-centre heading tape (degrees from the recording's forward, with a cyan square per teammate and an orange diamond for the person at their bearings), top-right points + round team map (tick ring, view wedge, cyan squares = teammates with heading, orange diamond = person, white triangle = you), bottom-left one-line key, bottom-centre honesty caption, bottom edge a 2 px progress line with the time; the replay transport is hidden on the phone preview; person = orange point ghost inside a thin chamfered bracket with a small two-line tag at its top-right (no leader). Eagle Eye facts came from an agent that downloaded the reveal clips and inspected frames (report in the session log): overlays straight on passthrough, 85-90 % of the frame empty, grey/white + cyan #18FFF5 / red-orange #F94D2C / mint, top heading arc with friendlies and hostiles marked, round minimap with tick ring and FOV wedge, 1-2 px chamfered person boxes with tiny tags, no glow. **Moving second POV**: `?campath=t:x,y,z@ax,ay,az;...` (eased keyframes on the replay clock, with `cam=fixed`) and `?feed=1` (a synthetic camera feed layer: recorded colours, every point, round 3.2x points, no HUD, black) so a virtual camera move stays registered: render the feed with the same path, blur it (ffmpeg gblur 7), then render the HUD layer over it. Real moving footage still needs the second phone recorded with Stray Scanner. `?hud=0` hides the HUD. 38 tests, build, smoke green. **Renders (05:00, one or two at a time: four parallel headless Chrome renders ran the 15 GB laptop out of memory and Claude Code's guard killed every background job including the dev server):** `hero_follow_ui_v2.mp4`, `hero_pov2_portrait_v2.mp4` (still, over the real clip), `hero_split_v2.mp4`, `hero_pov2_moving_black.mp4` (camera path, hologram over black; bare renders now composite over black via `Emulation.setDefaultBackgroundColorOverride`), `hero_pov2_moving_feed.mp4` (same path over the blurred synthetic feed `hero_pov2_feed_blur.mp4`), `demo-v2.mp4` (96 s: title / walk-in 0-9 s / split screen / card / moving hologram 0-32 s / end card; `demo-v2-web.mp4` 27 MB sent to the user). Camera path used: `9:-1.6,0.05,-2.75@0.46,-0.25,-3.05;14:same;22:-1.55,0.05,-2.4@0.46,-0.3,-2.3;30:-1.6,0.05,-2.9@0.46,-0.15,-3.5;38:-1.5,0,-2.1@0.46,-0.45,-1.7;48:back;57:back` |
| B14 | feedback round 2 (2026-09-20 05:30): bigger outline tags, a radial view selector, the person tag glitch, a minimap showcase clip | implemented. Outline tags 0.8 m wide (were 0.4), heavier face, hung above the box's left edge and clamped to eye height on tall boxes. **View wheel** (`src/wheel.js`): a translucent ring with three sectors Camera / X-ray / Natural (natural = the same reveal in the recorded colours), a white selection arc, the label in the centre; opens on the mode word or key `m`, keys 1/2/3 pick, tap outside closes; `?view=camera|xray|natural`; `omni.setView(view, progress)`; `npm run render -- ... --wheel-at "12:xray;32:natural"` scripts open (0.35 s), sweep + switch (0.9 s), close (0.3 s) on the replay clock. **Person tag glitch**: the tag was positioned inside the bracket's `onBeforeRender`, one frame late and after the sprite was sorted, so it lagged and popped during a camera move; now `Ghosts.face(camera, sceneRoot)` runs from the main loop before the render, in sceneRoot space. **Minimap showcase**: `?mapsize=<px>` renders only the map, centred over black, with scaled markers and a large key (`hero_minimap_showcase.mp4`, landscape, hero 9-45 s on the camera path). 38 tests, build, smoke green (smoke's direct `setXray` call now keeps `omni.view` in step). Renders (06:00): `hero_pov2_wheel.mp4` (moving second POV over the blurred feed, camera view, wheel picks X-ray at 12 s, Natural at 32 s, X-ray at 44 s), `hero_minimap_showcase.mp4` (landscape, map only, hero 9-45 s), `hero_pov2_portrait_v3.mp4`, `hero_follow_ui_v3.mp4`, `hero_split_v3.mp4`, `demo-v3.mp4` (128 s: title / walk-in / split / card / wheel shot 1-41 s / card / map showcase 0-22 s / end card; `demo-v3-web.mp4` 29 MB sent to the user). **`demo/` at the repo root (06:15)**: web-sized copies of the cut and every shot (each under 50 MB, 146 MB total) plus a README, with `!demo/*.mp4` carved out of the root `.gitignore` so teammates can watch them from GitHub; the masters stay in `node_modules/.cache/omni-render/` |
| stretch | second responder (viewer side): done 2026-09-19, see B9 (3); A still has to record the second take. `?renderer=spark` with @sparkjsdev/spark 2.x (peer three >= 0.180); Points stays the fallback | Spark after Phase 3 only |

Gate G1 (with Dev C): **passed 2026-09-19** against `origin/feat/scene-format-and-test-data` (unmerged at the time). Our validator exits 0 on C's `scenes/fake` (92,806 static points, 40 chunks, 13,344 alignment points, 70 ghost frames 5.0-11.9 s, identity first pose, wall_z -1.8, floor_y -1.3); the scene renders in our viewer with no console errors; C's `read_chunk` and `read_people` accept our `box` scene. One contract bug found and fixed on our side: `encodePeople` rounded centroids to 4 decimals and C's reader checks them to 1e-5, so centroids are now written as exact means (`box` regenerated). `git merge-tree` shows both teammate branches merge into main with no conflicts. C's branch also carries the pipeline branch (`feat/recording-to-scene-pipeline`, 41 tests pass here, 1 fails on an mp4 edit-list frame count with OpenCV 5.0, 1 skipped); no real scene exists yet.

## File map

```
viewer/
  vite.config.js         base /OmniSight/ (also in dev), dev on 127.0.0.1:5173, basic-ssl only in `--mode ssl`
  index.html             #pre card (status + error banner, outside the XR overlay) and #hud (dom-overlay root: caption, fps/points readout, staleness legend, controls)
  src/params.js          the only URL reader; sceneUrl(file) resolves under import.meta.env.BASE_URL
  src/debug.js           window.__omni, error banner, rolling fps
  src/format.js          parseChunk / parsePeople / concatPeople, pure ESM (Node tests import it)
  src/scene-data.js      mergeChunks, computeBounds, drawCountAt, poseIndexAt (pure, tested)
  src/scene-loader.js    fetches manifest, chunks, alignment.bin, trajectory.json, people.*; returns plain typed arrays
  src/points.js          PointCloud: THREE.Points + ShaderMaterial (aColor u8x4 normalized, aRadius, aTime; uClock, uIgnoreTime, uViewportH, uMaxPx, uSizeScale, uStaleStart 5, uStaleEnd 30, uRound, uBrightness)
  src/clock.js           ReplayClock: play/pause/restart/seek/speed, driven by the animation-loop timestamp, loops at duration
  src/hud.js             binds #hud controls to the clock, repaints at 8 Hz, beforexrselect guard, keyboard shortcuts (space, r, h, x, arrows); X-ray toggle, mode label (Camera / X-ray / Aligning), honesty caption with the rendering method
  src/modes/commander.js OrbitControls, dark background, floor grid, wall outline, dimmed alignment cloud, top-down toggle
  src/modes/ar.js        hand-written local-space session, support/permission failures, overlay, exit/re-entry, reset diagnostics
  src/benchmark.js       2 s warmup + 10 s XR-frame measurement, average and slowest half-second fps
  src/alignment.js       AR-only sceneRoot offsets, wall visibility, saved device offsets and URL precedence
  src/ghosts.js          Ghosts: one Points over the concatenated people arrays, setDrawRange per frame, additive red-orange, canvas-sprite label, hold + fade
  src/responder.js       Responders: one Responder (frustum LineSegments at poseAt, trail Line with setDrawRange, depthTest off, renderOrder 8/9) per source id, own colour + label with 2+ sources
  src/label.js           makeLabel({ color, width }).setText(title, sub): canvas tag sprite (accent bar, upper-case title, smaller second line) shared by the ghost label and responder labels
  src/outlines.js        Outlines: hand-annotated object boxes from <scene>/outlines.json as red fat-line wireframes with tags (reveal at first-seen time, age fade, through walls)
  src/minimap.js         setupMinimap + pure buildOccupancy: the team map canvas (scanned footprint by age, wall line, responders, person, you)
  scripts/fit-outlines.mjs  candidate outline boxes from a scene's points + a top-down PNG, for hand labelling
  src/portal.js          Portal: depth-only occluder quad on any wall plane (group frame: wall at z = 0, +Z outside) with a shader-cut hole whose radius animates (setOpen) + additive ring; update(camera, sceneRoot) moves the hole to the gaze hit and reports which side the camera is on
  src/main.js            boot: params -> loadScene -> PointClouds under sceneRoot -> clock -> mode -> hud -> setAnimationLoop
  scripts/omni-format.mjs   Node encoder: encodeChunk, encodePeople, writeScene
  scripts/make-scene.mjs    box room generator (--points --duration --person a:b --wall-z --floor-y --seed --name --sources N)
  scripts/validate-scene.mjs handoff gate; FAIL/WARN lines, exit 0/1; --budget N --strict-budget
  scripts/make-qr.mjs       `npm run qr`: public/qr.png for the deployed commander view (--scene, --url, --out, --width)
  scripts/phone-omni.mjs    `npm run phone`: adb-forwards Chrome DevTools on the USB Samsung and prints __omni plus a SAMSUNG.md benchmark row; `-- --watch 5 --for 10` logs a heat-soak line every 5 s
  scripts/load-scene.mjs    `npm run load -- <scene> [--t s] [--params "k=v&..."]`: headless commander load of one scene, JSON report + screenshot, exit 0 only with no errors (B5 acceptance)
  scripts/render-video.mjs  `npm run render -- <scene> [--params ... --fov --from --to --fps --width --height --dpr 2 --xray-at s --xray-fade s --bg clip --bg-offset s --bg-stretch f --stills t,t --capture --ui none --out --frames]`: deterministic frame-by-frame mp4 (or PNG stills) of a replay via headless Chrome + ffmpeg; default is the phone-screen preview (`ui=ar`, HUD at dpr 2) on the first-person camera, footage seeked per frame behind the page, scripted camera-view -> x-ray switch
  scripts/composite-video.mjs `npm run composite -- --render 3d.mp4 --outside wall.mp4 --cross <s> [--fade 2 --offset 0 --floor 0 --stretch 1]`: real wall footage over the 3D render, fading to transparent at the wall crossing
  scripts/resplit-scene.mjs  `--in <scene> --out <scene> --wall-z <z>`: re-bucket every point of a scene at another wall plane (fixes a take exported with the default -1.8) without rerunning the pipeline
  test/format.test.js, test/scene-data.test.js   node --test
  test/ar.test.js        mocked XR lifecycle and frame timing (not hardware verification)
  test/alignment.test.js transform, nudge precision, persistence, URL precedence, storage failures
  scripts/browser-smoke.mjs  dependency-free headless Chrome/Edge check; `npm run smoke` with dev server running, or OMNI_URL=<base> against Pages (skips the stress caps when that scene is absent)
  SAMSUNG.md            phone acceptance steps, budget URLs, results table and integration commands
  NUMBERS.md            measured numbers for the pitch (hour-13 handoff to C): URLs, fps table, scene stats, what not to claim
  public/scenes/box/     committed 20k-point generated room (default scene)
  public/scenes/stress/  gitignored 774k-point room from `npm run stress`
  public/scenes/fake/    Dev C's Python-generated room (merged at gate G1)
  public/qr.png          table-demo QR -> https://michaelyeh507.github.io/OmniSight/?mode=commander&scene=fake
  public/scenes/two/     committed 7.8k-point two-source fixture (`--sources 2`): second walkthrough from the same jig 1.5 s later, source_id 1 on the back-right corner
```

All planned viewer files now exist; B8 is polish, deploy and device checks.

## Technical decisions already made

- three pinned at 0.186.0; plain JavaScript; one merged BufferGeometry for all chunks, one draw call; `frustumCulled = false` on every big object; no attribute uploads after load. Playback changes only `uClock` and the draw range (`drawCountAt`: chunks are time-sorted, so the drawn set is a prefix).
- Point size: `gl_PointSize = aRadius * projectionMatrix[1][1] * uViewportH / -mv.z`, clamped to `[1, min(24, ALIASED_POINT_SIZE_RANGE)]`. `uViewportH` comes from `camera.viewport.w` on XR sub-cameras (three sets it) or the drawing-buffer height on flat screens.
- Hidden points (`aTime > uClock`) are parked at clip `(2,2,2,1)` with size 0. Never hide with fragment `discard`; round points are a `?round=1` toggle because `gl_PointCoord` discard costs early-Z on tile GPUs.
- Colors are sRGB bytes written straight to `gl_FragColor` (ShaderMaterial gets no color-space conversion). Staleness: age = `uClock - aTime`, full color under 5 s, `smoothstep` to a desaturated blue-gray by 30 s, hold.
- AR (B3): hand-written session start, not ARButton; `requestSession` rejected with `NotSupportedError` is retried once after `retryDelay` 800 ms inside the same tap (ARCore releasing the previous session), any other rejection fails immediately: `navigator.xr.isSessionSupported('immersive-ar')` -> from a tap `requestSession('immersive-ar', { requiredFeatures: ['local'], optionalFeatures: ['dom-overlay'], domOverlay: { root: hud } })` -> `renderer.xr.setFramebufferScaleFactor(params.fbscale)` -> `renderer.xr.setReferenceSpaceType('local')` -> `await renderer.xr.setSession(session)`. `local` space puts the origin where the phone is when the session starts, so start in the jig and hold still 2 s. `scene.background = null`, `alpha: true`, `antialias: false` in AR. Only `sceneRoot` moves for alignment; the camera is overwritten from the XR pose. Count `reset` events on the reference space in `__omni`.
- Dev loop: `npm run dev` on 127.0.0.1 (adb reverse targets IPv4 loopback), `npm run reverse`, Samsung opens `http://localhost:5173/OmniSight/?mode=ar&scene=box` (localhost is a secure context, no cert). Fallback `npm run dev:ssl` over the hotspot. Console via `chrome://inspect#devices` or `adb logcat -s chromium`.
- B3 budget runs add `&bench=1` to hold the entire scene at its end time. Tap **Measure 10 s** in AR; it warms up for 2 s, then reports average / minimum half-second fps and saves a serializable result in `__omni.benchmark`. Tracking loss, replay edits, changed settings, reference resets or session exit cancel the run. `budget` caps static points; the HUD includes any visible alignment points. Stress has 774,018 static points, so 800k and unlimited draw the same count. No Samsung performance claim until the table in `SAMSUNG.md` is filled.
- B4 starts in x-ray mode; **Align wall** reveals the complete, fresh `alignment.bin` and the sliders. **Show x-ray** hides only that cloud. Nudges transform `sceneRoot`, never the XR camera. Bounds are +/-2 m and +/-30 degrees. **Save offsets** writes `omnisight.alignment.v1` in this browser's localStorage (per origin/device); **Reset offsets** zeros and saves. Finite `ax/ay/az/ayaw` override saved axes, including explicit zero, and are clamped/rounded to the control steps. URL values are temporary until saved; an unchanged URL reapplies them on reload. Commander ignores both saved and URL AR offsets. Storage failures do not prevent AR.
- For comparable stress runs after testing nudges, use `&ax=0&ay=0&az=0&ayaw=0` to override arbitrary test offsets without overwriting the saved values. The phone currently stores x=0.04 m, y=-0.05 m, z=-1.74 m, yaw=0 degrees; these are control-test values, not a calibrated alignment.
- Portal (B7): oversized plane (40 m) at `wall_z + 0.02` with a 0.6 m hole, hole clamped to `PORTAL_REACH` 3 m from the viewer's foot on the wall (`clampWallHit`, tested) so the occluder always covers the visible wall, `MeshBasicMaterial({ colorWrite: false, depthWrite: true, side: DoubleSide })`, `renderOrder -1`; translate it so the hole sits where the gaze ray hits the wall plane in `sceneRoot` space. Ghosts, responder, ring and label are `transparent`, `depthTest: false`, ascending renderOrder so they show through the wall. `?portal=0` falls back to plain x-ray.
- Responders (B9, second-responder stretch on B's side): `splitTrajectory` groups the merged, time-sorted trajectory by `source`; one frustum + trail per source so a merged two-take scene from A's `--multi-source` export no longer zigzags one frustum between walkers. Colours from `RESPONDER_COLORS` (cyan, amber, magenta, green), labels from `manifest.sources[].label` and a legend row only with 2+ sources; caption switches to "Two recorded walkthroughs replayed together". `__omni.responders` is the array, `__omni.responder` stays source 0.
- Ghosts (B6): one `Points` over the concatenated people arrays, `setDrawRange(entry.start, entry.count)` of the latest entry with `t <= clock`; hold the last frame, fade over 15 s, label sprite at the centroid redrawn once per second.
- Ghost brightness (B9): on the real `room012_masked` take the additive ghost burned to white, twice over: points a metre from the lens overlap massively, and additive orange on top of bright cyan surfaces sums to white whatever the alpha. Ghosts now use **normal (over) blending** with a lighter core, so overlaps converge to orange; per-point alpha is also scaled by on-screen size (`(uRefPx / gl_PointSize)^2`, ref 10 px) and per frame by voxel occupancy (`sqrt(1.5 / overlap)`, overlap = 4·count·r²/(occupied 5 cm voxels·0.05²); the generated capsule sits at ~1.4 so it is unchanged, a torso-sized real frame at ~8-25 is dimmed). Ghost size cap 24 px.
- Demo video (B9, 2026-09-20): `?cam=follow` puts the commander camera on source 0's interpolated pose (frustum hidden, orbit/grid/axes/wall outline/cutaway off); `?cam=fixed&campos=x,y,z&camat=x,y,z` is a still virtual camera (same clean scene) and `?cutx=` hides points below an x (the near wall seen from a corridor), `?cuty=`/`?cutz=` override the doll-house clips; `?fov=` sets the vertical field of view (53 at 4:3 matches the iPhone 14 Pro colour camera, fx 1348 for 1440 px). `render-video.mjs` seeks the clock per frame and pipes PNGs to ffmpeg (libx264, crf 18); `composite-video.mjs` overlays the outside-wall footage with an alpha fade centred on the wall-crossing time (room012_masked crosses z = -1.8 at 28.7 s) and an optional floor opacity. Verified with a placeholder wall clip: opaque before, half through at the crossing, room only after. **Take 012 (shared 2026-09-20 02:09 UTC, copied to `data/raw/012`, gitignored)**: the recording starts inside the dorm room beside the door, not at a jig outside a wall, so its `rgb.mp4` is a first-person "how it works" plate (preview composite made: real video dissolving into the point cloud at 28.7 s, footage time-stretched x1.00411 to the odometry clock) but not an outside-wall view. The room is 2.9 x 4.9 m, door in the wall x = -0.69; `wall_z` -1.8 was the pipeline default and put 170k room points in `alignment.bin`, hence `room012_video` (re-split at wall_z 1.5, everything inside). Through-wall shot: fixed camera in the corridor `campos=-2.6,0,-1.4 camat=1.2,-0.5,-1.4 cutx=-0.55`, to be composited under footage of the real door wall filmed from that spot. **A proper jig take (outside the wall, facing it squarely) is still needed for the AR alignment demo.**
- Clean capture (B9): `body[data-capture="1"]` hides `#hud .panel` except `#caption`; set from `?capture=1` at boot and toggled by `h`. Screen recordings for shots 3 and 4 use it; the caption must stay on screen (honesty rule).
- Wall plane (B10): `parseWall(spec, wall_z)` in `scene-data.js` gives `{ normal, d }` with n·p = d on the wall and n pointing OUTSIDE (the viewer's side at the jig). `z:-1.8` is the contract default; room012's door wall seen from the corridor is `-x:-0.69`. The Portal group is placed so its local frame has the wall at z = 0 and +Z outside; the old z-plane maths (`intersectWallPlane`, `clampWallHit`) runs unchanged in that frame and the hit is reported back in sceneRoot space. The occluder is one 40 m quad whose hole is cut in the fragment shader (`uHole`, `uRadius`), so the radius animates without new geometry. Points get `uWallPlane` / `uWallClip`: in x-ray, with the camera outside, everything less than `WALL_CLIP_DEPTH` 0.14 m inside the plane is hidden (the wall's own plaster and anything outside), which is what `cutx=-0.55` did by hand for the corridor render; from inside the room nothing is clipped.
- X-ray state (B10): `omni.xray = { on, progress, animate }`; `omni.setXray(on, progress?)`. The loop animates `progress` toward `on` at 1 / 0.45 s unless a script pinned it (the render passes a frame-exact progress). `reveal = aligning ? 1 : progress`: static cloud visible only above 0, ghosts alpha x reveal, responders hidden at 0, portal `setOpen(reveal)` (iris). Off = the plain camera view; the legend hides (`body[data-xray="0"]`). AR still starts in x-ray unless `?xray=0`.
- Phone-screen preview (B10): `?ui=ar` in commander mode shows the AR HUD over a transparent page (scene background null, clear alpha 0, `body[data-ui="ar"]` transparent), hides the outside-wall cloud like AR x-ray does, enables the portal and hides Exit / Measure and the fps figure (a headless render has no frame rate to report). `render-video.mjs` injects a `<video>` behind the page from a tiny range-serving HTTP server and seeks it per frame (`offset + t / stretch`), so the composite happens in the browser exactly as on the phone: feed, GL layer, DOM overlay. `--dpr 2` lays the HUD out at width/2 CSS px and rasterises at 2x, the phone's own scaling. `composite-video.mjs` (footage fading over a render) is kept for the full-reveal variant.
- HUD (B10, design research 2026-09-20: Apple visionOS HIG, Microsoft MR typography/comfort, Magic Leap placement, Google ARCore UI, EBU R95 safe areas, MIL-STD-2525 colours; Eagle Eye itself only from written descriptions, no screenshot was fetchable): hue means class (cyan surfaces, orange person, amber = aligning, red only for errors), age is alpha/desaturation, never a hue shift; person is orange/amber, never red (red reads as hostile); dark backplate at 72 % with a cyan hairline, weights 600-700, upper-case tracked 11 px labels, tabular numerals, base 15 px (about 39 device px on the S21, above the 30 px floor); 5 % graphics-safe inset in the preview (`--pad: 5vmin`), centre of the screen kept clear; person tag = accent bar + PERSON + "seen now" / "last seen N s ago" + a leader line to the figure; legend = data-age bar (now / 5 s / 30 s+) + PERSON row, hidden while nothing is revealed; legend and replay transport share a bottom dock (`#dock`): stacked on a phone (legend above the transport, no overlap), legend bottom-left and transport bottom-right from 900 px wide; caption = "Replayed from a recorded walkthrough · point-based rendering · processed in N s" ("Gaussian splat rendering" only with `renderer=spark`).
- Visual direction from the user's reference images, applied in B6/B8: `LOOKS` in `points.js` map fresh -> stale onto a palette scaled by recorded luminance (x-ray cyan on black is the default; blueprint is dark-on-white for the commander); ghosts additive red-orange (solid red on blueprint); portal ring cyan. Commander cutaway (`CUTAWAY_HEIGHT` 0.9 m above the jig camera, and `wall_z - 0.08`) gives the doll-house view; never applied in AR.

## Integration gates and what B needs from A and C

| Hour | B needs | From | B hands back |
| --- | --- | --- | --- |
| ~1:30 | `common/omni_format.py`, `scenes/fake` per CONTRACT clarification 6 | C | validator, `box` scene for C's reader check, URL scheme |
| 3:00 | nothing | | point-budget table: device, XR framebuffer size, fbscale, fps at `?scene=stress&budget=200000|400000|800000` vs `budget=0` (delivered 2026-09-19 for the S21 Ultra: 30 fps camera cap at every count up to 774k) |
| ~6:00 | first validator-clean real scene | A | fps at A's count, alignment slider values |
| 8:00 | real `people.json`/`people.bin`; Pages source flipped to GitHub Actions | C | insurance clip of the through-wall view |
| 13:00 | masked scene with `floor_y`; QA slider values per device | A, C | deployed URL and QR text (delivered 2026-09-19: Pages live, `public/qr.png`), `__omni` numbers for the pitch (delivered: `viewer/NUMBERS.md`) |
| 16:00 | three hero scenes (`hero_small` if the download is slow on LTE) | A | freeze; only hero-exposed fixes after |

Cut order if behind (from the design doc): Spark, second responder, timeline scrubber, portal (plain x-ray stays), commander polish. Never alignment, ghosts or staleness.

## Session handoff, 2026-09-20 (end of the second session)

**On `main` (380b3e3, deployed, Pages green):** B0-B8, the table QR, `npm run phone`, `npm run load`, the pitch-tolerant validator, `__omni.firstPose`, capture mode, the S21 budget table, Enter AR retry, portal reach clamp, multi-source responders, `NUMBERS.md`.

**Uncommitted in `viewer/` (this session's demo-video work, all tests/build/smoke green):** `?cam=follow|fixed` + `campos/camat`, `?cutx/cuty/cutz`, `?fov`, `render-video.mjs`, `composite-video.mjs`, `resplit-scene.mjs`, `load-scene.mjs --params/--eval`, ghosts on normal blending with occupancy-normalised alpha, `uClipMin` in `points.js`; **session 3 (2026-09-20)**: x-ray on/off + iris, `?wall=`, `?ui=ar`, HUD redesign (`index.html`, `hud.js`, `label.js`, ghost leader line), wall clip in `points.js`, `render-video.mjs` with `--xray-at/--bg/--stills/--dpr`, portal + plane tests, smoke checks for the preview. Three **untracked scene folders** (`room012_masked`, `room013`, `room012_video`) are working copies pulled from C's branch or derived: delete them before merging, they must never be committed from here (`room012_video` is regenerated with `resplit-scene.mjs`).

**Next git step (the user runs it):** `Remove-Item` the three folders, `git switch -c b/merge-c-scenes`, `git add -- viewer`, commit, `git merge --no-edit origin/feat/people-run-and-spark-renderer`, `npm install` (lockfile must stay clean), `npm test`, `npm run build`, validate `room013` and `room012_masked`, `npm run smoke`, ff-merge to `main`, push, then check the Actions run (it runs `npm ci`). After that, `node scripts/resplit-scene.mjs --in public/scenes/room012_masked --out public/scenes/room012_video --wall-z 1.5` recreates the video scene (untracked).

**Teammates:** C's branch `feat/people-run-and-spark-renderer` (13c392e): real scenes `room013`, `room012_masked`, a Spark renderer behind `?renderer=spark` in our lane (reviewed: dynamic import, lockfile consistent, merges clean; not for the default path: no reveal, no staleness, no budget cap, drops the alignment cloud). A's branch `fix/offline-trajectory-registration`: voxel speedup and opt-in ICP are fine; its full-basis normalization must NOT merge (see B9 row 6); A was told. Neither teammate has pushed since.

**Take 012 (`data/raw/012`, gitignored, 160 MB):** the source of `room012_masked`. Starts inside the dorm room beside the door, so it is not a jig take: Phase 2's through-wall alignment on the phone still needs a recording that starts outside the wall facing it squarely. Room 2.9 x 4.9 m, door in the wall x = -0.69, `wall_z` -1.8 was the pipeline default (170k room points landed in `alignment.bin`).

**New takes (2026-09-20 ~01:20, from the teammates' Drive zip, unpacked into `data/raw/<id>`, gitignored):** all Stray Scanner 1920x1440 @ 60 fps of the same dorm room 314D. `9876363381` (38 s): long corridor approach, door, room with the person at the desk. `c9ae63f0c4_2` (57 s): corridor start, walks in, the person later stands up and walks around: **the hero take**. `8e2fc18402` (53 s): corridor start, empty room, clean furniture scan. `a2aa77eee6` (62 s): starts inside, out to the shared kitchen and back. Every new take decodes one video frame fewer than its odometry rows (A's loader refuses that), so the last odometry row and the last depth/confidence PNG of each were dropped in the raw copies. Dev C's person masks need `pip install ultralytics` (done here) and the gitignored `people_out/` folder to exist before `python -m people.run`; run it from the repo root with `PYTHONPATH` set (the shell cwd drifts). Masks land on even frames (stride 2), so the masked pipeline run uses `--stride 4`. The corridor takes exceed 1.5 M points at voxel 0.025: use `--voxel 0.04` (or a bigger `--max-points` for render-only scenes). **Camera convention (found 2026-09-20 02:30):** Stray Scanner's odometry is camera-to-world in OpenCV axes (+Z forward, +Y down); with the pipeline's default `--camera-convention arkit` every take comes out rolled 180 deg (validator: "first pose tilted 172-177 deg", floor above the walker, walker moving +Z). Main's pipeline on take 012 does the same; C's `room012_masked` is level because C exported with `--camera-convention opencv`. So every export AND every `people.run` (its ghosts use the same poses) takes `--camera-convention opencv`. Dev A's branch `fix/offline-trajectory-registration` does not fix this (its full-basis normalization only hides the roll by making frame 0 the identity); main's yaw-only normalization is right once the convention flag is set. A must make `opencv` the default or document it; the validator's tilt warning is the tell.

**Demo video state (`viewer/node_modules/.cache/omni-render/`, not in git):**
- `room012_corridor_1080p.mp4`: the through-the-wall 3D layer (fixed camera in the corridor, room revealing over 54 s, orange person, trail). `demo-throughwall-placeholder.mp4` is it under a stand-in wall (fade at 7 s over 4 s, floor 0.18): the intended effect, viewable now.
- `demo-1080p-preview.mp4`: the take's own `rgb.mp4` dissolving into the first-person reconstruction at 28.7 s (footage stretched x1.00411 to the odometry clock). A "how it works" shot.
- `room012_follow_1080p_v2.mp4` / `room012_follow_4x3.mp4`: first-person renders (the 4:3 one predates the ghost fix).
- `room012_corridor_ui_placeholder.mp4` (session 3, final cut 02:00 with the red outlines and the team map): the phone-screen version of the corridor shot: camera view with the HUD for 7 s, then the iris opens on the door wall and the room, the person tag, legend and caption show; stand-in wall behind it. 1,635 frames, no errors; check frames `mp4-frames/corridor-final-20.png`. Rendered against `vite preview` on port 4173 (`OMNI_URL=http://127.0.0.1:4173/OmniSight/`, static: immune to the dev server's page reloads; rebuild first so `dist/` carries the scenes).
- `room012_follow_ui.mp4` (session 3, final cut 02:00 with outlines and map): first person over the take's own `rgb.mp4` (stretched x1.00411), camera view until 8 s, then x-ray; fov 43.6. 1,635 frames, no errors; check frame `mp4-frames/follow-final-30.png`. Lens check with recorded colours: `stills-lens/`.
- `scripts/assemble-demo.mjs`: cuts title cards (dark, cyan rule, Segoe UI bold) and trimmed clips together with 0.5 s crossfades in one ffmpeg run.
- **`demo-v1.mp4` (03:20, 89 s, 1920x1080; `demo-v1-web.mp4` is the 29 MB copy sent to the user):** title / `hero_follow_ui` 0-9 s (corridor, camera view) / `hero_split.mp4` (48.5 s: first POV landscape left + `hero_pov2_portrait` right on the same clock from hero 9 s; built by the split ffmpeg command in the B12 notes below) / age card / `room012_corridor_ui_placeholder` 5-30 s / end card. Rebuild: the `assemble-demo` command line is in the session log; split = `ffmpeg -ss 9 -i hero_follow_ui.mp4 -i hero_pov2_portrait.mp4 -filter_complex "[0:v]scale=1312:738,pad=1312:1080:0:(oh-ih)/2:color=#0b0f14,setsar=1[l];[1:v]scale=608:1081,crop=608:1080,setsar=1[r];[l][r]hstack=2,format=yuv420p[v]" -map "[v]" -t 48.5 ...`.
- Missing: the real outside-wall clip, filmed from the corridor ~2.6 m from the door wall, chest height, landscape, still, 60 s. Then, from `viewer/`: `npm run render -- room012_video --params "cam=fixed&campos=-2.6,0,-1.4&camat=1.2,-0.5,-1.4&wall=-x:-0.69" --xray-at 7 --bg <clip> --out node_modules/.cache/omni-render/demo-corridor.mp4` (add `--bg-offset <s>` if the clip should start later; `--stills 5,7.2,9,20` first for a quick look). The old `npm run composite` fade path still works for a full-reveal variant.

**Lessons this session:** additive ghost points burn to white on real data (dense frames and bright cyan behind), so ghosts use over-blending now; a scene exported with the wrong `wall_z` hides half the room in `alignment.bin` (commander hides it under any clip and never reveals it over time), re-split instead of re-running the pipeline; the validator's identity-first-pose rule was wrong and pushed A into a tilting normalization; editing `src/` with the dev server up reloads the phone page and ends any AR session; never navigate the phone tab while `xrPresenting` (it cancels a measurement); Chrome exit code 21 means two runs shared a profile directory.

**Session 3 (2026-09-20): the AR UI for the demo, done on B's side (B10 row).** The phone screen now has a camera view and an x-ray view (HUD button, key `x`, `?xray=0`), the portal sits on any wall plane (`?wall=-x:-0.69` for room012's door wall), and `npm run render` produces the phone-screen video with the HUD, the footage behind it and a scripted switch. Both perspectives were checked frame by frame before, during and after the switch (`node_modules/.cache/omni-render/stills-*`). Not verified: the new HUD and the x-ray toggle on the phone itself (no AR session this session), and the portrait layout beyond a 1080x2400 headless still (`stills-portrait2/`, after the dock fix).

**Next:** (1) the real outside-wall clip -> one render command (above); (2) a jig take from A -> pipeline, validator, `npm run load`, B5 on the phone; (3) otherwise phone checks with the user: the new HUD in AR (portrait: caption + readout at the top, mode block, legend above the transport), the X-ray toggle and the iris, the Enter AR retry, the portal clamp, the orange ghost with its tag on `room012_masked`, `__omni.firstPose` pitched 30 deg, AR from the Pages URL, heat soak.

**Open on B's side:** phone verification of the Enter AR retry, the portal clamp and the new ghost look; `__omni.firstPose` test (phone pitched 30 deg at session start: tiltDeg ~30 means `local` is gravity-aligned and the contract stands); AR from the deployed Pages URL over Wi-Fi; insurance clip; heat soak (`npm run phone -- --watch 5 --for 10`); iPad Safari check; B5 the moment a jig take exists.

## Physical checklist (only the user can do these)

Detailed B3/B4 acceptance, full budget URLs, results table and copy-paste git handoffs: `SAMSUNG.md`. Dev B committed and pushed implementation 5ce7d44. The agent never ran commit, merge or push; all agent file changes stay inside `viewer/`. After USB debugging was enabled, ADB authorized the S21 Ultra and `npm run reverse` succeeded. Chrome opened the box URL in a secure context, reported immersive AR support and showed an enabled Enter AR button with no viewer errors.

Devices: Galaxy S21 Ultra (SM-G998U1, Chrome 147.0.7727.137, Google Play Services for AR 1.54.260890093) and Galaxy A54 (not yet tested). Dev B confirmed the live camera feed, point-cloud box, HUD and stable anchoring while moving on the S21 Ultra, followed by nudge/yaw/x-ray controls and saving offsets. Dev B explicitly tested control behavior only, without lining the synthetic box up to the real room. Once the phone was awake, the agent reloaded the box page and verified that the saved offsets restored exactly with no viewer errors. The 200k stress URL then loaded 200,000 points at t=60 with zero alignment overrides and preserved the saved values. No timed FPS result is available yet. Keep separate measured budgets and saved alignment values for each phone. Remaining: explicit Exit AR/re-entry check, stress budget, A54, human commander check and real-scene registration (B5).

- Samsung: update "Google Play Services for AR"; run https://immersive-web.github.io/webxr-samples/immersive-ar-session.html in Chrome; enable Developer options + USB debugging; accept the RSA prompt; install the Samsung USB driver if `adb devices` is empty; disable One UI Auto Blocker if USB commands are refused.
- Confirm the Quick-panel screen recorder captures an AR session with camera feed and HUD before hour 8 (`scrcpy --record` is the fallback).
- Windows Defender prompt on `--host 0.0.0.0`: allow on Private and Public. The adb-reverse loopback path avoids the firewall entirely.
